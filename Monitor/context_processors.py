def Monitor_context(request):
    #from .views import ecgDetails, Gpatient, GpID, Grequest
    #from .ecgDetails import contextTransfer
    #print(Gpatient, GpID, Grequest)
    #e = ecgDetails(Grequest, GpID, Gpatient)
    #print(getattr(e, context))
    #from ecgDetails import patients#context, currentSamples
    #print(patients, context, currentSamples)          
    #return {'ecgDetails': ecgDetails}